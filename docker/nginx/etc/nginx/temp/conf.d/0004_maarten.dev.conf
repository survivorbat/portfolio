server {
    listen 8443 ssl http2 default_server;

    server_name {{ server_name }};

    root /var/www/html;

    index index.html;

    location / {
    	expires {{ cache_max_time }};
        try_files $uri @php;
    }
}
