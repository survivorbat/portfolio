server {
    listen 8443 ssl http2 default_server;

    root /var/www/html/maarten.dev;

    index index.html;

    location / {
    	expires {{ cache_max_time }};

    	{% if env | default('prod') == "dev" %}
		proxy_pass http://portfolio_node:3000;
    	{% endif %}
    }
}
