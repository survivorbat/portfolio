{% block nav %}
    <nav class="nav--element">
        <ul class="nav--list">
            {% for item in layout.menu.items %}
                <li class="nav--item">
                    <a href="{{ item.href }}" class="nav--anchor">{{ item.displayName }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endblock %}