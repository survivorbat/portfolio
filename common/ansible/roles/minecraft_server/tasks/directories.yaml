- name: Ensure {{ plugin_folder }}
  file:
    state: directory
    path: "{{ plugin_folder }}"

- name: Ensure {{ config_folder }}
  file:
    state: directory
    path: "{{ config_folder }}"

- name: Ensure {{ mod_folder }}
  file:
    state: directory
    path: "{{ mod_folder }}"
